services:
  - type: web
    name: the-chamber
    runtime: docker
    plan: free
    healthCheckPath: /_stcore/health
    envVars:
      - key: PORT
        value: "8501"
      - key: DATABASE_URL
        fromGroup: chamber-secrets
